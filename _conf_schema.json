{
  "poll_interval": {
    "description": "轮询間隔（秒）",
    "type": "int",
    "hint": "每隔多少秒检查一次 Minecraft Feedback 的更新",
    "default": 60,
    "required": false
  },
  "target_sessions": {
    "description": "目标会话列表",
    "type": "list",
    "hint": "使用 /mcupdate_add_session 命令添加会话，或在此处手动填写会话 ID",
    "default": [],
    "required": false
  },
  "admin_ids": {
    "description": "管理员 ID 列表",
    "type": "list",
    "hint": "允许执行管理员命令（如推送消息）的用户 ID",
    "default": [],
    "required": false
  }
}
